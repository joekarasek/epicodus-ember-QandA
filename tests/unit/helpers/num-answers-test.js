import { numAnswers } from 'ember-qand-a/helpers/num-answers';
import { module, test } from 'qunit';

module('Unit | Helper | num answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numAnswers([42]);
  assert.ok(result);
});
